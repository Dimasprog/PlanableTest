const component = require('./screen');
const componentTypes = require('./screen.types');
const componentStyles = require('./screen.styles');
const index = require('./index');

module.exports = [component, componentTypes, componentStyles, index];
